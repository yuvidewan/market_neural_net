# --- core ---
numpy>=1.26
pandas>=2.1
polars>=1.0
pyarrow>=14.0
duckdb>=1.0
omegaconf>=2.3

# --- data acquisition ---
requests>=2.32
yfinance>=0.2.55
tqdm>=4.66

# --- ml (installed already on this box; pin loosely) ---
torch>=2.2
scikit-learn>=1.3

# --- testing / quality ---
pytest>=8.0
pytest-cov>=5.0

# --- experiment tracking (optional, add when Phase 3 starts) ---
# mlflow>=2.14
